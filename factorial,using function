#include<stdio.h>
long int fact(int n)
{
   if(n>=1)
   return n*fact(n-1);
   else
   return 1;
    }

int main()
{
    int a;
    printf("enter the number\n");
    scanf("%d",&a);
    
    printf("factorial of %d is %d",a,fact(a));
}
